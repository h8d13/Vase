#!/bin/bash

# Check if glmark2 is installed
if ! command -v glmark2 >/dev/null 2>&1; then
    echo "Error: glmark2 not found. Install it with: sudo pacman -S glmark2"
    exit 1
fi

# Run glmark2 and output to bench.log
echo "Starting GPU benchmark..."
glmark2 2>&1 | tee -a bench.log